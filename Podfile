# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HackagridClient' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HackagridClient
  pod 'Alamofire', '~> 4.8.0'

  target 'HackagridClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HackagridClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
